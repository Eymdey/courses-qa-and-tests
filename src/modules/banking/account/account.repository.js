// src/modules/banking/account/account.repository.js

// Fonction pour créer un compte en base de données
export const createAccountInRepository = async (accountData) => {
  // La logique SQL viendra ici plus tard
};

// Fonction pour récupérer les comptes d'un utilisateur
export const getAccountsFromRepository = async (userId) => {
  // La logique SQL viendra ici plus tard
};

// Fonction pour supprimer un compte
export const deleteAccountInRepository = async (accountId) => {
  // La logique SQL viendra ici plus tard
};
